# Preset for week-11-platform-ops — committed, contains NO secrets.
# `make live` sources this. Your keys live in ../.env (gitignored).

ANTHROPIC_MODEL=claude-haiku-4-5
OPENAI_MODEL=gpt-4o-mini
LAB_USD_CAP=0.05
