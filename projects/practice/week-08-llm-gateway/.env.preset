# Preset for week-08-llm-gateway — committed, contains NO secrets.
# `make live` sources this. Your keys live in ../.env (gitignored).

ANTHROPIC_MODEL=claude-haiku-4-5
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small
LAB_USD_CAP=0.10
