# Preset for week-09-quality-platform — committed, contains NO secrets.
# `make live` sources this. Your keys live in ../.env (gitignored).

ANTHROPIC_MODEL=claude-sonnet-4-5
OPENAI_MODEL=gpt-4o
LAB_USD_CAP=0.20
