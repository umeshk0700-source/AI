# Shared environment for every day's lesson.
# Install into the uv venv with:  uv pip install -r requirements.txt
numpy
matplotlib
tiktoken

# notebook tooling
jupyterlab
ipykernel
nbconvert
nbformat

# Week 3 — fine-tuning (Day 09)
torch>=2.2
transformers>=4.44
peft>=0.12
datasets>=3.0
accelerate>=0.33

# Week 5 — embeddings (Day 13+)
sentence-transformers
duckdb
anthropic

# Practice labs (projects/practice/) — real API clients + test tooling
openai>=1.40
anthropic>=0.40
python-dotenv>=1.0
tenacity>=8.2
voyageai>=0.2
pytest>=8.0
fastapi>=0.110
httpx>=0.27
pydantic>=2.5

# Week 11 — MCP (Day 31). Pinned <2: the v1 FastMCP / ClientSession API the lesson uses.
mcp<2
